# ================================================================
# ExamSolver Frontend — Environment Variables
# ================================================================
# Sao chép file này thành .env (dev) hoặc .env.production (build)
# ================================================================

# URL gốc của backend API (bao gồm /api nếu có)
# Ví dụ local:      http://localhost:8080/api
# Ví dụ production: http://171.244.142.43/api
#
# Nếu để trống → Vite sẽ dùng proxy /api → http://localhost:8080
# (chỉ hoạt động khi chạy npm run dev, không hoạt động sau khi build)
VITE_API_BASE_URL=
